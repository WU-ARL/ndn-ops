#!/bin/sh -e
#
# rc.local
#
# This script is executed at the end of each multiuser runlevel.
# Make sure that the script will "exit 0" on success or any other
# value on error.
#
# In order to enable or disable this script just change the execution
# bits.
#
# By default this script does nothing.

# start ndnx and OSPFN
#/home/ndnops/ndn-ops/named-data/running_version/start_scripts/startAllDaemons.sh > /tmp/ndnx.start.log

# start ndnx-tlv and OSPFN. running_version now includes ndnx-tlv
#/home/ndnops/ndn-ops/ndnx-tlv/start_scripts/startAllDaemons.sh > /tmp/ndnx-tlv.start.log
/home/ndnops/ndn-ops/named-data/running_version/start_scripts/startAllDaemons.sh > /tmp/ndnx-tlv.start.log


exit 0
